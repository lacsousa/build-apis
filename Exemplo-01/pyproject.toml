[project]
name = "exemplo-01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "openai>=1.0.0",
    "pre-commit>=4.6.0",
    "python-dotenv>=1.0.0",
    "ruff>=0.15.13",
]
